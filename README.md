# PXP  Chrome Extension

Do the following.

Clone the repository.

Then open chrome and goto chrome://extensions

Followed by clicking on Developer mode.

Then select Load Unpacked extension add the directory.